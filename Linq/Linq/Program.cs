using System;
using System.Linq;

namespace Linq
{
    internal class Program
    {
        static void Main()
        {
            Teste.teste();
            ////data source -- fonte de dados
            //int[] vect = { 1, 2, 3, 4, 5, 6 };

            ////define the query - Definir a consulta
            //var Result = vect.Where(x => x % 2 == 0).Select(x => x * 10).ToList();

            ////Excute the query - excecutar a consulta
            //foreach( var x in Result) Console.WriteLine(x);
        }
    }
}
